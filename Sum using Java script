function sum(...args) {
    // Your implementation
    return args.reduce((total, curr) => total + curr, 0);
}

//For the purpose of user debugging.
sum(100, 200, 300, 400);

module.exports = sum

//Key points - Rest parameters, reduce function
